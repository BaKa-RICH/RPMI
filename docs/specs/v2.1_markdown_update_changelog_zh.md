# v2.1 Markdown Update Changelog 中文版

本次生成了 8 个 v2.1 Markdown 文件，对当前上传的 v2 文件进行定向更新。

## 已固化的核心修改

1. 所有 evidence package 必须同时报告 reservation denominator 与 demand denominator。
2. 禁止用 `failed_reservation_rate` 单独代表 merge success。
3. Wave 6A.0 增加 A1/A2/A3：指标、文件命名、positive RCMV package。
4. Wave 6A+ 增加 S6_productive、positive RCMV hardening、S5 no-action inventory 判别。
5. Gate D0 增加 A/B/C 判定规则和 claim allowed / claim forbidden。
6. Wave 7 继承 denominator 规则。
7. Human Roadmap 增加人类如何读 failed reservation vs unserved demand。
8. Claim-Evidence Matrix 增加指标误读风险和论文禁止表述。

## 未处理项

当前消息没有上传 06、07、08、09、10 五个 Markdown 文件，因此本次未生成它们的 v2.1 文件。后续上传后可按同一 denominator 规则轻微继承。
